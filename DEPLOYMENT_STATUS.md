# Статус развертывания 21day.club

## ✅ Выполнено

1. **Сервер настроен:**
   - Установлены необходимые пакеты (Docker, Nginx, Certbot, UFW)
   - Настроен firewall (порты 22, 80, 443)
   - Установлен Node.js 20.20.0

2. **Приложение развернуто:**
   - Исходный код скопирован на сервер: `/var/www/21days.club`
   - Зависимости установлены (`npm install`)
   - Приложение собрано (`npm run build`)
   - Собранные файлы находятся в `/var/www/21days.club/dist`

3. **Nginx настроен:**
   - Конфигурация для домена `21days.club`
   - Настроена раздача статических файлов из `dist/`
   - Включено gzip сжатие
   - Настроено кеширование статических ресурсов
   - Добавлены security headers

4. **Файлы загружены:**
   - Все файлы проекта загружены на сервер
   - Права доступа настроены (www-data:www-data)

## ⏳ Ожидает выполнения

1. **Настройка DNS:**
   - Добавить A-записи:
   - `21day.club` → `195.133.63.34`
   - `www.21day.club` → `195.133.63.34`

2. **Установка SSL сертификата:**
   После настройки DNS выполнить:
   ```bash
   ssh root@195.133.63.34
   certbot --nginx -d 21day.club -d www.21day.club --non-interactive --agree-tos --email admin@21day.club --redirect
   ```

3. **Настройка переменных окружения Supabase:**
   Приложение использует переменные окружения для Supabase:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_PUBLISHABLE_KEY`
   
   Эти переменные должны быть заданы во время сборки. Если нужно пересобрать с новыми переменными:
   ```bash
   ssh root@195.133.63.34
   cd /var/www/21days.club
   export VITE_SUPABASE_URL="your_url"
   export VITE_SUPABASE_PUBLISHABLE_KEY="your_key"
   npm run build
   chown -R www-data:www-data /var/www/21days.club
   systemctl reload nginx
   ```

## 📁 Структура на сервере

```
/var/www/21days.club/
├── dist/              # Собранное приложение (отдается Nginx)
├── src/               # Исходный код
├── public/            # Публичные файлы
├── supabase/          # Supabase функции и миграции
├── package.json       # Зависимости
└── ...
```

## 🔧 Полезные команды

```bash
# Пересобрать приложение
cd /var/www/21days.club
npm run build
chown -R www-data:www-data /var/www/21days.club
systemctl reload nginx

# Проверить логи Nginx
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/access.log

# Проверить статус Nginx
systemctl status nginx

# Проверить SSL сертификат
certbot certificates
```

## 🌐 Доступ к приложению

- HTTP: http://21day.club (работает после настройки DNS)
- HTTPS: https://21day.club (работает после установки SSL)

## 📝 Примечания

- Приложение собрано и готово к работе
- Для работы приложения необходимы переменные окружения Supabase (если они не были заданы при сборке, нужно пересобрать)
- SSL сертификат будет установлен автоматически после настройки DNS

